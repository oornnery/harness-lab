---
name: _dynamic
description: Dynamic context injected per-turn into agent instructions.
version: 1
---

Session id: {session_id}
Active persona: {persona_name}
Workspace mode: {mode}
Approval mode: {approval_mode}

Workspace snapshot:
{workspace_summary}

Recent session signals:
{recent_events}
